---
declared-services:
  seams-cloudant-service:
    label: cloudantNoSQLDB
    plan: Shared
applications:
  disk_quota: 512M
  host: seams-${random-word}
  name: seams
  path: .
  domain: mybluemix.net
  instances: 1
  memory: 256M
  random-route: true
  command: node app.js
  services:
    - seams-cloudant-service
